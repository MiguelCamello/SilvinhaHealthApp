git config --global user.name "MiguelLima"
git config --global usar.email "mlimacamello@gmail.com"

-- Criar um projeto React Native Expo
npx create-expo-app@latest SilvinhaHealthApp --template blank

-- Restaurar Projeto
cd SilvinhaHealthApp
npm i

npx expo start
npx expo start --android

--instalando os pacotes
cd SilvinhaHealthApp
npx expo install expo-router react-native-safe-area-context react-native-screams expo-linking expo-contents expo-status-bar

